type: object
required:
  - email
  - type
properties:
  email:
    type: string
    format: email
  type:
    type: string
    enum: [magic_link, otp]
  redirectUrl:
    type: string
    format: uri
    description: "Where the magic link should redirect (handled by frontend)."


