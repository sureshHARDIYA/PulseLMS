type: object
description: "For OTP-based verification; magic links are handled by redirect."
required:
  - email
  - otp
properties:
  email:
    type: string
    format: email
  otp:
    type: string
    minLength: 4
    maxLength: 12


