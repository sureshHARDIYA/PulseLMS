type: object
required:
  - id
  - slug
  - name
properties:
  id:
    type: string
    format: uuid
  slug:
    type: string
    pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
  name:
    type: string
  description:
    type: string
  sortOrder:
    type: integer
    minimum: 0


