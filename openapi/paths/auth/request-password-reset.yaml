post:
  tags: [Auth]
  summary: Request password reset
  description: "Send a password reset email with a reset link/token."
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/AuthResetRequest.yaml'
  responses:
    '202':
      description: "Reset email queued for delivery."
    '400':
      $ref: '../../components/responses/Problem.yaml'


