post:
  tags: [Auth]
  summary: Start passwordless login
  description: "Start a passwordless login via magic link or email OTP."
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/AuthPasswordlessStartRequest.yaml'
  responses:
    '202':
      description: "Magic link/OTP email queued."
    '400':
      $ref: '../../components/responses/Problem.yaml'


