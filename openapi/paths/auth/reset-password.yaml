post:
  tags: [Auth]
  summary: Reset password
  description: "Confirm password reset using a token from email."
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/AuthResetConfirmRequest.yaml'
  responses:
    '204':
      description: "Password reset successfully."
    '400':
      $ref: '../../components/responses/Problem.yaml'
    '401':
      $ref: '../../components/responses/Problem.yaml'


