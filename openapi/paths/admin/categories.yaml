post:
  tags: [Admin Categories]
  summary: Create category
  security:
    - bearerAuth: []
  x-roles: [admin]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/CategoryCreate.yaml'
  responses:
    '201':
      description: "Created."
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/Category.yaml'
    '400':
      $ref: '../../components/responses/Problem.yaml'
    '401':
      $ref: '../../components/responses/Problem.yaml'
    '403':
      $ref: '../../components/responses/Problem.yaml'

