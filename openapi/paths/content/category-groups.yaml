get:
  tags: [Content]
  summary: List category groups
  description: "Top-level groups for organizing categories."
  responses:
    '200':
      description: "Category groups."
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/CategoryGroup.yaml'


